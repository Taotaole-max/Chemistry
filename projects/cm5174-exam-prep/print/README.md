# PDF 生成

两份 PDF 由脚本从源文件生成，改内容后重跑即可。

| 文件 | 作用 |
|---|---|
| `tex.py` | LaTeX 子集 → HTML 转换器（分式真正上下堆叠、根号、装框公式） |
| `data.py` | 30 道题的结构化数据（题干、选项、答案、解析） |
| `pdfbuild.py` | markdown / 题目数据 → 打印版 HTML（A4 版式、分页控制） |

```bash
pip install pymupdf markdown playwright
apt-get install -y fonts-noto-cjk      # 缺 CJK 字体中文会变方块
python3 pdfbuild.py                    # 生成 notes.html / bank.html
python3 topdf.py                       # Chromium 渲染成 PDF
```

`topdf.py` 用 Playwright 的 `page.pdf()`，带页眉页码。

## 版式上的两个决定

- **知识点**：每个 Lecture `break-before:page`，从新页开始，方便按讲复习
- **题库**：卡片允许跨页，但 `.q-top`（题干 + 选项）锁成整块不拆。
  最初整张卡片 `break-inside:avoid`，导致一页只放得下一道题、31 页里全是空白；
  改成现在这样压到 21 页

## 分讲题库（301 题）

| 文件 | 作用 |
|---|---|
| `../bank/L01.py` … `L10.py` | 每讲的题目源数据，各 30 题以上 |
| `../bank/normalize.py` | 合并十讲 → `bank_all.json`，并做两件事（见下） |
| `bank_pdf.py` | 由 `bank_all.json` 生成 184 页打印版 HTML |

```bash
cd bank && python3 normalize.py          # 生成 bank_all.json
cd .. && python3 print/bank_pdf.py       # 生成 bank_all.html
python3 topdf2.py                        # 渲染成 PDF
```

### normalize.py 解决的两个质量问题

出题时无意中埋了两个坑，全局质检才发现：

1. **只给正确选项加了 `<b>`** ——301 题里有 200 题如此，等于把答案印在题面上。
   normalize 统一剥掉选项里的加粗。
2. **答案分布严重失衡** ——原始分布 A/B/C/D = 60/147/76/18，蒙 B 能得 49%。
   normalize 把正确项挪到当前用得最少的位置，重新平衡到 76/75/75/75。

**但不能无脑打乱**：有 34 题的解析里写了「选项 A 是…」「选项 C 就是这个陷阱」这类引用，
打乱会让解析对不上。normalize 用正则 `选项\s*([ABCD])` 识别这些题并**跳过**，
只重排其余 267 题，最后断言锁定题的选项与答案未被改动。

### 每题的五要素

题目 / 答案 / 解析 / **知识点** / **出处**。出处精确到讲义页码与页标题
（如「讲义 Lectures_1-4 · p.6「Molecular Weight of Polymers」」），可直接翻回原文核对。
页码索引由 PyMuPDF 逐页提取标题建立，不是凭印象写的。

## 知识点总结（完整版，34 页）

`03_知识点总结_完整版.md` → `notes_full_pdf.py` → `topdf3.py`（在 scratchpad）

由早期的 `01_知识点整理.md` 扩充而来，加了三样东西：

1. **公式速查表**（放最前面）——按讲次汇总全部公式，一讲一页
2. **每讲的讲义页码范围**——可直接翻回原文核对
3. **跨讲串联 12 条**（放最后）——做 301 题时梳理出的主线，综合题几乎全出自这里

### fix_subscripts.py 与一个隐蔽的正则坑

正文里有大量裸写的 `T_m`、`M_n`，markdown 不会渲染成下标，需要转成 `<sub>`。
但替换必须**跳过公式和代码段**，否则会破坏 LaTeX。

第一版的跳过正则写成 `(\$[^\$\n]+\$|`[^`]+`)`，**漏了 `$$...$$` 块**。
后果不是"少替换一处"这么简单：`$$` 会被当成一个残缺的 `$...$` 匹配，
**奇偶配对从此错位**，文档后半部分的正文全被误判成公式而整段跳过。
表现是前 52 处替换成功、后面的一处不改——很容易误以为是漏配了某几个符号。

正确写法是**把 `$$...$$` 放在交替的第一位**（正则交替是最左优先）：

```python
SKIP = re.compile(r'(\$\$.*?\$\$|\$[^\$\n]+\$|`[^`]+`)', re.S)
```

### 另一个坑：列表前必须有空行

python-markdown（即使开了 `sane_lists`）要求列表前有空行，否则会把
`- xxx` 当成上一段的续行，渲染成带散落 `-` 的一整段。
组装脚本里加了一步：凡是列表/表格行的前一行是正文，就自动插入空行。

## 进阶应用题（41 题，34 页）

`../bank/HARD.py` → `normalize_hard.py` → `hard_all.json` → `hard_pdf.py` → PDF

用户反馈「考试很难，要有思考含量、强调运用」，于是按**七类难点**重新设计，
而不是把基础题加大数字：

| 类型 | 题数 | 难在哪 |
|---|---:|---|
| 多步链式计算 | 10 | 2–4 个概念串联，跳步就做不出 |
| 逆向求解 | 5 | 指数、系数都要反着用（如 f = 2^(1/ν)） |
| 判别与证伪 | 6 | 哪条结论站不住、从数据能推出什么 |
| 数量级与标度 | 4 | 指数差如何放大成数量级差 |
| 实验设计 | 5 | 需要什么量 + 样品有什么限制，两道筛子 |
| 跨讲综合 | 6 | 认出贯穿多讲的同一条主线 |
| 陷阱换皮 | 5 | 同一个坑换场景（如键角改 120°） |

沿用 `normalize.py` 的两道质检：剥离选项加粗、平衡答案分布
（原 6/14/14/7 → 11/10/10/10），并跳过解析中引用了选项字母的 17 题。

### 几道自己比较满意的题

- **H05**：由 PE 晶胞参数 a=7.42、b=4.95 Å 算 (110) 面间距再求 2θ，
  得 **21.6°**，与讲义给的实测强峰 21.5° 吻合——这就是 XRD 指标化的真实做法。
- **H17**：N 从 100 增到 10000，混合熵**只降 1%**。
  这对「分子量越高越难溶」的通常说法是个重要修正：
  真正起作用的是 χ_c 逼近 0.5 与相图移向稀溶液，而不是熵还在大幅下降。
- **H01**：等摩尔混合 PDI=1.67，等质量混合 PDI=3.02。
  同样两个组分，配法不同分布宽度差近一倍。
